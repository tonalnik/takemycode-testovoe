{
	"name": "testovoe",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build:frontend": "cd frontend && npm run build",
		"build:backend": "cd backend && npm run build",
		"start:backend": "cd backend && npm run start",
		"run:backend": "cd backend && npm run build && npm run start",
		"run": "npm run build:frontend && npm run run:backend"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"@types/node": "^25.0.3"
	}
}
